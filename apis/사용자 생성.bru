meta {
  name: 사용자 생성
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/test/users
  body: json
  auth: none
}

body:json {
  {
    "name": "{{name}}",
    "email": "{{email}}",
    "phoneNumber": "010-0000-0000"
  }
}

script:pre-request {
  // pre-request script
  const random = Math.floor(Math.random() * 1000000);
  email = `load${random}@test.com`;
  name = `부하 테스터${random}`
  
  bru.setVar("email",email)
  bru.setVar("name", name)
}

tests {
  1000
}
